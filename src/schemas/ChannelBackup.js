const { Schema, model } = require("mongoose");

const schema = new Schema({

Kodlara bakarak kanal backup nasil aldiriyorum bak semayi doldur dostum
// Coded by kei
// Coded by kei
// Coded by kei

});

module.exports = model("channelbackup", schema);