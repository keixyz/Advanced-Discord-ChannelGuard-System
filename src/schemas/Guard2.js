const { Schema, model } = require("mongoose");

const schema = new Schema({

Oyle hatasiz bisi yok dostum yaz buraya guvenli id semasi
// Coded by kei
// Coded by kei

});

module.exports = model("guard2", schema);